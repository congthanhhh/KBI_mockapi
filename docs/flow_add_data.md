# Flow moved to backend-api-endpoint skill

This repository keeps the add/change data workflow in the AI-facing skill:

`kbi-mock-api/agent/skills/backend-api-endpoint/SKILL.md`

Codex and other agents should use the `Data/Contract Change Workflow` section
there as the source of truth when adding or changing mock collections, fields,
relationships, endpoint payloads, seed data, docs, or frontend-visible API
surfaces.
